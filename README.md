
  # Design UI Color Scheme

  This is a code bundle for Design UI Color Scheme. The original project is available at https://www.figma.com/design/2KoBJddtxBUXW7Fu41jDfy/Design-UI-Color-Scheme.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  